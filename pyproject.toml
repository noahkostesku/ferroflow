[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ferroflow"
version = "0.1.0"
description = "Distributed tensor computation scheduler"

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "."
manifest-path = "crates/python/Cargo.toml"
